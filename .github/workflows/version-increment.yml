name: Version Increment
run-name: ${{ github.actor }} is updating version number in master
on:
    push:
      branches:
        - master
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: npm version patch