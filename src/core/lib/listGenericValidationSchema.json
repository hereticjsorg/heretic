{
    "type": "object",
    "properties": {
        "fields": {
            "type": "array",
            "items": {
                "type": "string",
                "enum": []
            },
            "minItems": 1,
            "uniqueItems": true
        },
        "sortField": {
            "type": ["string", "null"],
            "enum": []
        },
        "searchText": {
            "type": ["string", "null"],
            "maxLength": 64
        },
        "sortDirection": {
            "type": ["string", "null"],
            "enum": ["asc", "desc"],
            "default": "asc"
        },
        "itemsPerPage": {
            "type": "integer",
            "minimum": 1,
            "maximum": 1000,
            "default": 30
        },
        "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
        }
    },
    "required": ["fields"]
}
