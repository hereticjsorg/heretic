export default class {
    constructor(config, db) {
        this.config = config;
        this.db = db;
    }

    async process() {
        // Do something
        // eslint-disable-next-line no-console
        console.log("Executing core/_home");
    }
}
