<if(state.failed && component.mongoEnabled)>
    <section class="section">
        <div class="container">
            <div class="notification is-danger">
                <t>signOutError</t>
            </div>
        </div>
    </section>
</if>
<if(!state.failed && component.mongoEnabled)>
    <hloading key="loading"/>
</if>
<if(!component.mongoEnabled)>
    <div class="notification is-warning">
        <t>requestedFeatureDisabled</t>
    </div>
</if>
